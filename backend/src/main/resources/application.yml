spring:
  datasource:
    url: jdbc:mysql://localhost:3306/GYCcom
    username: root
    password: 19530507
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
  application:
    name: blog-backend
server:
  port: ${SERVER_PORT:8080}

logging:
  level:
    org.hibernate: debug
