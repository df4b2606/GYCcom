# Environment Variables Example
# Copy this file to .env and update the values with your actual credentials
# NEVER commit the .env file to version control

# Spring Profile (dev, prod)
SPRING_PROFILES_ACTIVE=dev

# Server Configuration
SERVER_PORT=8080

# Supabase Database Configuration (for production)
# Use Transaction Pooler (port 6543) for better concurrency in production
# Direct connection: jdbc:postgresql://db.your-project.supabase.co:5432/postgres
# Transaction Pooler: jdbc:postgresql://db.your-project.supabase.co:6543/postgres (recommended)
# 
# IMPORTANT: These environment variables are REQUIRED when running in production mode
# No default values are provided in application-prod.yml for security reasons
# 
# FOR RENDER DEPLOYMENT:
# Set these environment variables directly in Render dashboard:
# Dashboard -> Your Service -> Environment -> Add Environment Variable
# 
# SUPABASE_DB_URL=jdbc:postgresql://your-supabase-host:6543/postgres?user=your-user&password=your-password
# SUPABASE_DB_USERNAME=your-username  
# SUPABASE_DB_PASSWORD=your-password
#
# Example format (replace with your actual values):
# SUPABASE_DB_URL=jdbc:postgresql://your-host.supabase.com:6543/postgres?user=your-user&password=your-password
# SUPABASE_DB_USERNAME=your-username
# SUPABASE_DB_PASSWORD=your-password

# Local Database Configuration (for development)
# These have default values in application-dev.yml but can be overridden
LOCAL_DB_URL=jdbc:postgresql://localhost:5432/gyccom
LOCAL_DB_USERNAME=gyc
LOCAL_DB_PASSWORD=your-actual-local-password